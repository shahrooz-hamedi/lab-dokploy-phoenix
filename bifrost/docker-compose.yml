services:
  bifrost:
    image: maximhq/bifrost:v2.2.2
    restart: unless-stopped

    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8080
      LOG_LEVEL: info
      LOG_STYLE: json
      BIFROST_ENCRYPTION_KEY: ${BIFROST_ENCRYPTION_KEY}

    volumes:
      - bifrost-data:/app/data

    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true

volumes:
  bifrost-data:
    driver: local